---
- name: Restart cron
  ansible.builtin.service:
    name: "{{ cron_service_fact_key | default(cron_service) }}"
    state: restarted
  when:
    - (cron_service_fact_key | default('')) | length > 0
